extends ../layout
block content
  .w3-main(style="margin-left:340px; margin-right:40px")
    .w3-row-padding
      .w3-half
        each img in panel.images
          button layout
          table.table
            tr
              each list in layoutList
                th= list
          button shape
          table.table
            tr
             th 
             th
          img.img-rounded.img-responsive(src=img style='width:100%' id='img' onclick='addButton(this)')


  script.
    function addButton(element){
      if($('element'))
      var table = document.createElement("table");
      var node = document.createNode("This is new.");
      para.appendChile(node);
      
  //    $('element').addClass
    }
    $(document).ready(function(){
      $("button").click(function(){
        $("table").toggle();
      });
    });